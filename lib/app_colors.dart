import 'package:flutter/material.dart';

class AppColors {
  // Main background (cream)
  static const background = Color(0xFFFEF9F0);

  // Primary deep green from logo
  static const primaryGreen = Color(0xFF144D3B);

  // Gold accent from logo (logo text etc.)
  static const accentGold = Color(0xFFB3881B);

  // Light greenish wave at bottom (no longer used, but kept in case)
  static const waveBackground = Color(0xFFCDCEB9);

  // Subtle gray/brown for small text
  static const subtleText = Color(0xFF7C7C73);

  // ---- Dashboard / general layout ----
  static const dashboardBackground = Color(0xFFE5E5E5); // outer grey background
  static const cardBackground = background;             // inner cards

  // Tabs
  static const tabInactive = Color(0xFF9AA0A6);

  // ---- Login screen ----
  static const loginButton = Color(0xFFFFA54A); // orange login button

  // ---- Tags / chips (for dashboard place cards later) ----
  static const chipBgGreen = Color(0xFFE1F3E8);
  static const chipBgBlue = Color(0xFFE1F0FF);
  static const chipBgOrange = Color(0xFFFFE4C2);

  // Detail page background (sample color you sent)
  static const locationDetailBackground = Color(0xFFF7F5DF);
  // Orange-ish chip background for tags (UNESCO / Adventure etc.)
}
